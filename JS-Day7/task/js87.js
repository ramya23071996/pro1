function Person(name, age) { 
    this.name = name; 
    this.age = age; 
return
}
let per1 = new Person("Arun",25); 


console.log(`User Name is:${per1.name}`);
console.log(`User age is:${per1.age}`);

